标准流的特性
1、分行、块级元素，并且能够display转换
2、块级元素（block）：默认独占一行，不能并列显示，能够设置宽、高，宽度为父盒子的100%。例如：div、p、标题元素（h1-h6）、列表元素（ul li、dl dt dd）
3、行内元素（inline）:默认并排显示，不能设置宽、高，宽度为内容的宽度。例如：span、a、b、i
4、margin-bottom 和 margin-top 塌陷，以最大值为准